## Setup

```
$ mise install

# Follows optional virtual env setup
$ python -m venv venv
$ . venv/bin/activate.fish # each time, `deactivate` to exit

$ pip install -e .[dev]

# To start jupiter
$ jupyter lab
```
